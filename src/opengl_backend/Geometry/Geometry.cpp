#include "Geometry.h"

namespace Cold {
    Geometry::Geometry(const std::initializer_list<VertexBufferSPtr> &vertex_data, void *index_data, u32 index_size)
    {
        
    }
    Geometry::~Geometry()
    {
    }
}